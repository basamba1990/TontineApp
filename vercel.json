{
  "framework": null,
  "buildCommand": "npx expo export",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
