{
  "framework": null,
  "buildCommand": "npx expo export:web",
  "outputDirectory": "web-build",
  "installCommand": "pnpm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
